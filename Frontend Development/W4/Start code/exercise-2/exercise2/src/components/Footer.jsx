let note = "You can find the React doc at https://react.dev/"

function Footer() {
    return(
        <footer className="block">
            <p>{note}</p>
        </footer>
    );
}

export default Footer;